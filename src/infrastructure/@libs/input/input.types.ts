export type CLIquestionInputPayloadType = {
    question: string
    errorMessage: string
}

export type CLIselectInputPayloadType = {
    question: string
    errorMessage: string
    options: string[]
}